---
tags: ["type/prd", "domain/ai获客", "kw/殡葬", "kw/AI获客", "kw/内容创作", "kw/数字人", "kw/功能需求"]
keywords: ["殡葬", "AI获客", "内容创作", "数字人", "功能需求"]
artifact: prd-detail
version: "1.0"
created: 2026-06-24
status: draft
---

# PRD · 功能需求明细（殡葬 AI 获客 V1，基于原型）

> 配套：[[PRD-殡葬AI获客-V1]]（总纲）｜ [[产品设计-殡葬V1-页面与字段]]（字段/状态）｜ 原型 `prototype/index.html`
> 说明：逐**功能点**给需求描述（行为 / 业务规则 / 状态与异常 / 依赖）。每条 FR 可独立验收。
> 全局口径：客户自助 SaaS · Web 端 · 我方开通账号 · 行业固定殡葬 · 公司账号多成员。

---

## 0. 登录与会话

**FR0-1 账号登录**
- 描述：用户用我方分配的账号（手机号/邮箱）+ 密码登录；**无自助注册入口**，页面注明"账号由服务方开通"。
- 规则：登录成功后进入工作台；登录态携带 公司ID / 用户ID / 角色（老板=管理员 / 运营）。
- 状态：账号或密码错误给出错误提示；支持"记住登录"。

**FR0-2 找回密码**
- 描述：忘记密码 → 输入账号 → 获取验证码 → 重置密码。
- 规则：验证码有有效期；重置后需用新密码登录。

**FR0-3 退出登录**
- 描述：左栏底部用户区点击退出，回到登录页。

---

## 1. 工作台（总览）

**FR1-1 关键指标卡**
- 描述：展示 本月产能（条）、待发布（条）、知识库事实（条）、数字人形象（个）。
- 规则：数据来自各模块实时统计；数字人形象显示"已生成 X / 上限"。

**FR1-2 最近作品**
- 描述：列出最近作品（标题、形态、平台、合规状态、时间），可"全部 →"跳作品库；行可"编辑/导出/查看"。
- 规则：表格右侧操作列与各列对齐，不右飘。

**FR1-3 待办**
- 描述：聚合"N 条含合规提醒待处理""N 条草稿未生成可恢复"，提供"去处理/恢复"入口。

**FR1-4 快速开始**
- 描述："粘贴参考开始创作""逛爆款库"两个入口，分别跳内容创作 / 爆款抓取。

**FR1-5 顶部信息**
- 描述：顶栏显示本月用量、当前公司（演示仅一家，预留切换）。

---

## 2. 内容创作向导（核心，5 步）

> 全程为一条可保存的草稿；中途退出可在工作台恢复。顶部步骤条可点击跳转已完成步骤。

### Step 1 · 参考输入
**FR2-1 参考来源切换**
- 描述：三种来源——**粘贴链接 / 粘贴文案 / 来自爆款库**，切换时显示对应输入区。
- 规则：链接抓取仅抖音 best-effort；失败则提示改用"粘文案/上传"。"来自爆款库"跳爆款页带回。

**FR2-2 行业固定**
- 描述：行业字段固定显示"殡葬"，不可改（V1 单行业）。

**FR2-3 解析与拆解触发**
- 描述：点"拆解"后进入解析（解析链接→转写→拆解）。
- 状态：解析中显示进度；解析完成进入 Step2；失败给出原因与重试/改输入。

### Step 2 · 拆解编辑
**FR2-4 行业逻辑拆解**
- 描述：系统按殡葬逻辑拆出 **钩子 / 结构 / 口播脚本 / 分镜**。
- 规则：每项可点"编辑"就地修改、"保存"落定（编辑态高亮）。

**FR2-5 可迁移公式沉淀**
- 描述：展示该爆款的"可迁移公式"，拆解后自动沉淀进「殡葬爆款模板库」。

### Step 3 · 平台与配置
**FR2-6 目标平台多选**
- 描述：勾选抖音 / 小红书 / 视频号 / 快手（至少一个）；**每个平台单独生成适配内容**。

**FR2-7 调用配置**
- 描述：选择"调用知识库范围"（全部/按分类）与"选用提示词"（我的风格之一）。
- 规则：知识库供事实、提示词供风格；二者职责不交叉。

**FR2-8 合规规避开关**
- 描述：合规规避默认开启且**锁定不可关**（系统殡葬行业规则）。

### Step 4 · 生成结果
**FR2-9 多平台分版生成**
- 描述：生成后按平台 tab 展示，切换 tab 内容随平台变化（标题/正文-口播/标签均不同）。
- 状态：生成中显示进行态；**部分平台失败可单独重试**。

**FR2-10 结果可编辑**
- 描述：标题、正文/口播、标签均可编辑。

**FR2-11 合规提醒（非阻断）**
- 描述：检测到违禁表达高亮提醒并给改写建议（如「灵堂」→「告别厅」）。
- 规则：可"采纳改写"（替换文本，提醒转为已处理/绿色）或"忽略"；**不阻断保存/导出**。

**FR2-12 出口选择**
- 描述：可"就到脚本/图文"（存作品库结束）或"→ 数字人成片"（进 Step5）。

### Step 5 · 数字人成片
**FR2-13 形象/声音选择**
- 描述：选用形象（分组：我的生成形象 / 默认公模形象）与声音（音色库/复刻）。

**FR2-14 口播稿与样式**
- 描述：口播稿默认取 Step4 脚本可改；选择字幕/背景模板；显示预计时长。

**FR2-15 成片生成（异步）**
- 描述：点"生成成片"提交蝉镜合成任务（异步），提示"完成后通知"，成片回作品库。
- 状态：成片中/成功/失败（可重试）。

---

## 3. 爆款抓取（接蝉妈妈）

**FR3-1 三种取数模式**
- 描述：顶部切换 **按关键词搜索 / 按对标账号 / 热门榜单**，分别显示对应操作区。
  - 关键词：输入框 + "搜索抓取"
  - 对标账号：账号列表 + "添加账号" + "抓取该账号爆款"
  - 热门榜单：选行业分类（殡葬·寿衣/骨灰盒/一条龙/习俗科普）+ "出热门榜"

**FR3-2 筛选维度**
- 描述：共用筛选——平台（抖音数据最全 / 小红书覆盖有限）、时间（近 7/30/90 天）、排序（点赞/播放/评论/涨粉）。

**FR3-3 爆款列表**
- 描述：卡片展示封面、标题、平台、播放/点赞/评论、**爆款倍数**。
- 规则：**爆款倍数按账号基线自算**（蝉妈妈给原始指标，不直接给爆款标签）；用相对基线非绝对数。

**FR3-4 卡片操作**
- 描述：每条"拆解"（带入内容创作 Step2）或"收藏"（可切换已收藏）。

**FR3-5 配置对标源**
- 描述：弹窗一次配齐 **关键词（逗号分隔）+ 对标账号（链接/名称）+ 平台**，保存生效。

**FR3-6 数据合规说明**
- 描述：页面注明数据来自蝉妈妈（合规取数非自爬）、抖音全/小红书限、接口/价格以商务对接为准。
- 状态：抓取中/抓取失败/无结果（空态引导配置对标源）。

---

## 4. 知识库（只存事实）

**FR4-1 多格式上传（唤起本地选择）**
- 描述：点"上传资料"或拖拽区**唤起本地文件选择**，支持 word/txt/excel/csv/pdf/音频/视频；音视频自动转写。
- 状态：上传中→处理中（转写/结构化）→完成；失败（格式不支持/转写失败）可见。

**FR4-2 结构化 / 非结构化区分**
- 描述：每条资料标注类型——**结构化**（excel/csv，按字段入库，可精确填充）或**非结构化**（文档/转写，分段走检索）。
- 规则：上传自动判类型（表格类=结构化，其余=非结构化）。

**FR4-3 分组管理**
- 描述：左侧分组栏（全部 + 各分组 + 新建分组）；可"新建分组""录入结构化"；资料归属分组。
- 规则：点分组按分组过滤；分组计数自动更新。

**FR4-4 类型筛选**
- 描述：按"全部类型 / 结构化 / 非结构化"过滤，与分组过滤叠加；显示"共 N 项"。

**FR4-5 结构化预览**
- 描述：结构化资料可"查看"，弹窗以字段表预览（如 产品/材质/规格/价格）。

**FR4-6 只存事实约束**
- 描述：知识库仅存事实（产品/价格/习俗/讲究/话术），风格归提示词，明确分工。

---

## 5. 提示词

**FR5-1 风格提示词管理**
- 描述：客户可新建/编辑多个风格提示词（名称+内容），可启用；用于生成时调口吻。

**FR5-2 合规规避（系统只读）**
- 描述：行业合规规避规则集由系统预置、版本化、**只读不可改**（殡葬规则集 vX）。

---

## 6. 数字人（接蝉镜）

**FR6-1 上传视频生成形象（克隆）**
- 描述：点"上传视频生成形象"**唤起本地视频选择**，提交蝉镜克隆（约 10s 训练，异步）。
- 状态：训练中（0→100%）→可用；失败可重试。

**FR6-2 形象配额与单独计费**
- 描述：**每企业最多 2 个"生成形象"**（克隆/定制占额度）；头部显示"已生成 X / 2"。
- 规则：达上限再上传→**拦截并弹"增购形象额度"**（单独计费，示例 ¥199/个，确认后额度+1）。

**FR6-3 默认公模形象**
- 描述：默认形象（蝉镜公模库）**可直接用于生成，不占额度、不单独收费**；每个可"用它生成"。

**FR6-4 声音管理与试听**
- 描述：声音来自音色库或 **5s 样本复刻**（"+ 复刻声音"唤起本地音频选择，复刻中→可用）。
- 规则：每个声音可"▶ 试听"（播放音色样例）。

**FR6-5 形象展示**
- 描述：形象以头像缩略图展示；加载失败兜底人像占位图。

**FR6-6 申请定制**
- 描述：从零设计原创虚拟形象走"申请定制"（提交需求，离线定制），不在产品内自建设计器。

---

## 7. 作品库

**FR7-1 列表与筛选**
- 描述：列出作品（标题/平台/形态/合规/发布状态/操作）；可按平台、形态（脚本/图文/成片）、合规、发布状态筛选，显示计数。

**FR7-2 导出**
- 描述：所有平台均可"导出"（下载/复制），供客户自己发。

**FR7-3 发布（双模式）**
- 描述：发布按账号的自动发开关决定——**自动发**（有 API：抖音）直接发；**无 API**（小红书/视频号）退化为"导出+提醒手动发"。
- 状态：发布状态含 草稿/已导出/已发布/待手动发/发布失败（失败可重试）。

**FR7-4 平台限制说明**
- 描述：注明自动发仅限有开放 API 平台，不吹"全平台一键自动发"。

---

## 8. 用量

**FR8-1 用量计数**
- 描述：按类型计数——爆款抓取 / 参考转写 / 内容生成 / 数字人成片；按月×类型累计，工作台顶部显示本月用量。
- 规则：V1 仅计数不扣费；预留配额/已用/剩余结构。

**FR8-2 形象额度（单独计费）**
- 描述：单列"数字人形象额度（已生成 X / 2）"，注明单独计费（与口播分钟分开），提供"管理形象"入口与增购。

---

## 9. 成员管理（公司账号）

**FR9-1 成员列表**
- 描述：展示公司信息与成员（姓名/账号/角色/状态）；角色含 老板=管理员 / 运营。

**FR9-2 成员增减与启停**
- 描述：管理员可"添加成员"（姓名+手机+角色）、对成员"停用/启用"。
- 规则：管理员本人不可被停用。

---

## 10. 平台账号绑定

**FR10-1 账号绑定**
- 描述：展示已绑定平台账号（平台/账号/授权状态/用途）；可"绑定新账号""授权"。

**FR10-2 自动发布开关（按账号控制）**
- 描述：**每个平台账号一个"自动发布"开关**，在本页控制是否自动发。
- 规则：**仅有开放 API 的平台（抖音/企业号）可开**；小红书/视频号开关**禁用**并提示"无接口，仅手动"。

---

## 全局非功能与规则（约束）

- **合规红线**：爆款数据走蝉妈妈（禁自爬）；私域走企微官方；合规预审为提醒非阻断 + 人工复核兜底。
- **采购计费**：数字人成片（~2.36 元/分钟）、形象（单独计费）、爆款数据（蝉妈妈）均按量采购，定价须覆盖。
- **异步与通知**：转写、数字人成片为长任务，后台处理 + 通知中心，不阻塞操作。
- **空态引导**：知识库/提示词/数字人/作品库/爆款 各有空态引导（替代砍掉的新手引导）。
- **数据模型**：所有资产带 租户ID × 行业ID 双维度（SaaS 基因）。

## Open（需确认）
- [ ] 形象增购定价、口播分钟定价、套餐结构
- [ ] 蝉妈妈小红书覆盖度、蝉镜/蝉妈妈接口字段（商务对接）
- [ ] 抖音发布资质归属
- [ ] 成员由管理员自助加（当前默认）vs 我方开通
