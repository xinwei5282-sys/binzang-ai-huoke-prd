---
tags: ["type/技术方案", "domain/ai获客", "kw/殡葬", "kw/知识库", "kw/rag", "kw/事实校验", "kw/精确召回"]
keywords: ["殡葬", "知识库", "rag", "事实校验", "精确召回"]
---

# 技术方案：知识库精确召回与事实校验（殡葬 AI 获客 V1）

> **版本**：v1.0 ｜ **状态**：草稿（架构评审用）｜ **日期**：2026-06-25
> **负责**：架构 — 架构师 ｜ 关联 PRD：[[PRD_知识库]] / [[PRD_数字人]] / [[PRD_内容创作]] / [[PRD_提示词]]
> 上层：[[产品设计-殡葬V1-页面与字段]] §九（可行性）｜ [[产品规划-殡葬V1]] §四（厂商成本）
> 性质：回答「知识库能不能**准确**抓取企业事实喂给内容生成 / 数字人」——给出**可实现的架构、选型、关键机制与评测方法**。

---

## 0. 一句话结论

**能实现，且全是成熟技术栈、无需攻关。** 但"准"不是 RAG 默认就有的，是**双路分治 + 事实校验**这套设计换来的：价格/规格走结构化精确查询（零幻觉），习俗/话术走向量语义召回，生成后再做一道事实比对。偷懒全塞向量库 → 一定不准；按本方案做 → 价格类可做到准确、叙述类可控。

---

## 1. 问题定义

内容生成与数字人口播要"事实准确、像这家公司说的话"。事实分两类，**对准确性的要求天差地别**：

| 事实类型 | 例子 | 容错 | 错了的后果 |
|--|--|--|--|
| **硬事实**（精确） | 价格 ¥2,880、七件套、真丝材质 | **零容错** | 报错价 = 客诉/纠纷；数字人念错 = 像老板承诺，翻车 |
| **软叙述**（语义） | 寿衣讲究单数、习俗由来、话术口吻 | 可容错 | 措辞偏差影响有限 |

**核心矛盾**：向量检索（语义相似度召回）天然适合软叙述，但对硬事实（尤其数字）会"猜错"。**所以不能用一套机制处理两类数据。**

---

## 2. 核心设计原则

1. **双路分治**：硬事实走结构化精确查询，软叙述走向量语义召回，物理分开。
2. **事实系统注入、LLM 只组织语言**：把召回的事实作为"槽位值"注入，LLM 负责把它写顺，不负责"想起"事实——降幻觉的根本手段。
3. **生成后校验兜底**：文案里的数字/价格，规则比对是否来自知识库，对不上即标红（复用合规预审的"提醒"机制）。
4. **关键事实人工确认**：高代价输出（数字人成片）前，对价格/承诺类口播稿加确认勾选。

---

## 3. 整体架构（数据流）

```
                    ┌─────────────── 入库层 ───────────────┐
Excel/CSV ─────────▶│ 表格解析 → 字段化 → 实体归一 → 结构化事实库(DB)  │  ← 硬事实
Word/TXT/音视频 ───▶│ (ASR转写) → 清洗分段 → embedding → 向量库       │  ← 软叙述
                    └──────────────────────────────────────┘
                                     │
生成请求(选题/平台/提示词) ──────────▶│
                    ┌─────────────── 检索层 ───────────────┐
                    │ ① 槽位抽取：这条文案要哪些事实？(价格槽/习俗槽) │
                    │ ② 价格槽 → 结构化精确查询(SQL/KV，命中即准确值) │
                    │ ③ 习俗槽 → Hybrid检索(BM25+向量)→rerank→top-k段 │
                    └──────────────────────────────────────┘
                                     │
                    ┌─────────────── 生成层 ───────────────┐
                    │ Prompt = 合规约束 + 风格 + [注入的事实槽] + 拆解骨架 │
                    │ LLM 组织语言（事实来自注入，不靠模型记忆）          │
                    └──────────────────────────────────────┘
                                     │
                    ┌─────────────── 校验层 ───────────────┐
                    │ 数字/价格抽取 → 比对结构化事实库 → 不一致标红提醒    │
                    │ 合规词库预审（违禁词）→ 提醒+改写建议               │
                    └──────────────────────────────────────┘
                                     │
              ┌──────────────────────┴───────────────────┐
        图文/脚本(直接用)                         数字人口播稿
                                          → 含价格/承诺时人工确认勾选 → 蝉镜成片
```

---

## 4. 入库层（决定准确率的源头）

### 4.1 结构化路（硬事实）
- **解析**：Excel/CSV 用 openpyxl/pandas 读表头映射字段（产品/材质/规格/价格）。
- **脏表兜底**：表头识别不了 → 触发 [[PRD_知识库]] §4.3 的"字段确认"步骤，人工对齐后入库。
- **存储**：结构化事实库（关系表或 KV：`company_id + 实体 + 属性 → 值`），**不进向量库**。
- **实体归一**（关键）：建别名表，把"寿衣套装/真丝七件套/七件套寿衣"归一到同一实体 key。殡葬产品 SKU 少，别名表人工可维护（V1 几十条量级）。

### 4.2 非结构化路（软叙述）
- **转写**：音视频走 ASR（Whisper / 讯飞，长音频切分），转写文本**入库前人工校对**降误差。
- **分段**：按语义/标题切 chunk（500–800 字，重叠 80–120），保留来源文件溯源。
- **向量化**：中文 embedding 模型，写向量库（RAGFlow 内置）。

---

## 5. 检索层

### 5.1 槽位抽取
生成前用 LLM/规则判断"这条文案需要哪些事实"：
- 涉及报价/选购 → 价格槽、规格槽（走结构化）
- 涉及科普/讲究 → 习俗槽、话术槽（走向量）

### 5.2 结构化精确查询（硬事实）
- 按实体（归一后）精确查结构化事实库 → 命中返回**准确值**；未命中 → **返回空 + 提示"知识库无此项，请补充或留空"**，**绝不交给 LLM 编**。
- 这是零幻觉路径，本质是数据库查表。

### 5.3 Hybrid 检索 + 重排（软叙述）
- **BM25（关键词）+ 向量（语义）混合召回** → 比单向量召回率高、抗"语义漂移"。
- **rerank 重排**：用 reranker 对召回片段精排，取 top-k（3–5 段）喂 LLM。
- 带**来源溯源**，便于校验与展示依据。

---

## 6. 生成层（降幻觉的核心）

Prompt 组装顺序（优先级从高到低）：
```
[合规约束(词库)] > [风格提示词] > [注入的结构化事实槽:价格=¥2,880,规格=七件套] > [向量召回片段] > [拆解骨架]
```
- 事实槽以**明确键值**注入（"七件套真丝寿衣，¥2,880"），指令 LLM **只能使用注入的数字，不得编造/修改**。
- 向量片段作为叙述素材，允许改写润色。

---

## 7. 校验层（兜底，防 LLM 自作主张）

1. **数字/价格回检**：正则抽取生成文案里的金额/数量 → 比对结构化事实库 → 不一致或来源不明 → **标红提醒**（"此价格未在知识库中找到来源"），走和合规提醒同一套"提醒非阻断"交互。
2. **合规预审**：命中合规词库违禁词 → 提醒 + 改写建议（见 [[PRD_后台_合规词库]]）。
3. 两道校验都是**提醒式**，客户可采纳/坚持，但留记录。

---

## 8. 数字人专项（高代价输出，额外一道闸）

数字人不直接查知识库——它念的是内容创作已生成的**口播稿**。风险在"放大效应"（老板本人形象+声音说错价=像郑重承诺、成片改不动）。所以：
- 口播稿进入数字人 Step5 前，若**含价格/承诺类事实** → 强制**人工确认勾选**（"已核对价格无误"）才放行合成。
- 建议补进 [[PRD_数字人]] 的设计约束与验收（见本方案 §11 落地动作）。

---

## 9. 技术选型（均成熟、可控）

| 层 | 选型 | 说明 |
|--|--|--|
| 向量/检索框架 | **RAGFlow** | [[产品规划-殡葬V1]] 已选；内置分段/向量/hybrid/溯源；GraphRAG 留 V2 |
| 中文 embedding | bge-m3 / bge-large-zh 类 | 中文检索主流，可私有化 |
| 重排 rerank | bge-reranker 类 | 显著提升 top-k 精度 |
| 结构化存储 | 关系库（PG/MySQL）表 + 别名表 | 价格类精确查询，零幻觉 |
| ASR 转写 | Whisper / 讯飞 | 长音频切分，有算力成本 |
| LLM 生成 | DeepSeek / 通义 / Kimi 官方 API | 按 token，[[产品设计-殡葬V1-页面与字段]] §九 |
| 多租户隔离 | 按 `company_id` 隔离索引与表 | 防跨公司泄露 |

> 全部为成熟开源/商用组件，**无自研算法攻关项**。

---

## 10. 准确率怎么量化（评测方法 = 真正的"确认"）

光说"准"没用，要可度量。建议建**评测集**：

| 指标 | 定义 | 目标 |
|--|--|--|
| 硬事实准确率 | 生成文案中价格/规格与知识库一致的比例 | **≥ 99%**（走结构化，应接近 100%） |
| 硬事实幻觉率 | 编造/错误数字占比 | **趋近 0**（校验层拦截） |
| 软叙述召回相关性 | 召回片段人工判定相关比例 | ≥ 85% |
| 未命中正确留空率 | 知识库无数据时未编造的比例 | 100% |

**评测做法**：构造 30–50 条殡葬测试问法（含价格类/习俗类），跑生成，人工标注。这就是下一步 POC 要落的事——本文档是 POC 的设计依据。

---

## 11. 风险与落地动作

### 11.1 风险与兜底
| 风险 | 兜底 |
|--|--|
| Excel 表结构杂解析错 | 字段确认步骤 + 人工校正 |
| 实体别名对不齐致查不到 | 别名归一表（小行业人工可维护）|
| LLM 擅自改数字 | §7 生成后数字回检标红 |
| 向量召回不相关 | hybrid + rerank + 溯源人工可见 |
| ASR 转写错误入库 | 入库前人工校对 |
| 知识库空仍硬生成套话 | 未命中留空提示 + Step3 空库引导 |

### 11.2 建议补进 PRD（本方案发现的强约束）
1. [[PRD_知识库]] 增设计约束：**价格类强制结构化、精确查询优先于向量、未命中留空不编造**；验收加"硬事实零幻觉"。
2. [[PRD_数字人]] 增设计约束：**含价格/承诺口播稿成片前人工确认**；验收加该项。
3. [[PRD_内容创作]] §4.3/4.4 标注：生成走"事实注入 + 生成后回检"。

### 11.3 落地步骤
1. （本文）架构评审通过 → 2. 借 `沉香AI标杆` RAG 趟坑经验 → 3. 跑 §10 评测集 POC，实测准确率 → 4. 达标则按本方案接入；不达标按短板迭代（多半在实体归一/分段策略）。

---

## 12. 相关
- [[PRD_知识库]] / [[PRD_数字人]] / [[PRD_内容创作]] / [[PRD_后台_合规词库]]
- [[产品规划-殡葬V1]]（RAGFlow 选型、厂商成本）
- `沉香AI标杆`（内部 RAG 先行项目，借经验）
