---
tags: ["type/索引", "domain/ai获客", "kw/殡葬", "kw/AI获客", "kw/prd索引", "kw/双轨"]
keywords: ["殡葬", "AI获客", "prd索引", "双轨"]
---

# 殡葬 AI 获客 V1 · PRD 索引（双轨 + 优先级排期）

> **【2026-06-28 现状对齐】** 本轮按两条已拍板全局口径就地更新：①**素材库 = 知识库内的一个子类型/视图**（数据层一个库，统一上传→AI 自动判类：结构化事实/非结构化/素材[图视]），不是独立的第二个库；②提示词归后台（口播/视频/拆解/约束等运营维护）。本轮改动：C1→v0.4、C2→v0.2；批次3 提示词配对补注。
> **【2026-06-30 再更新·覆盖上条②】** 提示词改为 **v2.0 三层模型**：系统基线（后台维护·**商家端只读可见**）+ 商家微调层（**商家可编辑**）+ 生成参数（后台按商户·商家可调·合规锁定）。商家端「提示词微调」页**恢复**；后台新增「模型参数」页（按场景×按商户）。详见 [[PRD_提示词]] §11。
> **【2026-07-05 更新·老板定调内容生成两缺口】** ①**出片确认门升级两级**：确认脚本 → 按分镜生成**首帧图**（素材库召回零成本/阿里云文生图约几毛/张）→ **确认分镜图后才烧钱出视频**；模板③ Seedance 段主路径改「文生图→i2v」（C1→v0.7）。②**图文补「图文预览」展示**：配图按配图脚本生成/召回（三级降级不硬凑），**图片本体多平台一套共用、文字层（图注/标题/正文/标签）按平台适配**，导出 zip 手动发（A1→v1.4）。原型已同步。

> 参考 SCRM PRD 编写逻辑：**一模块一份 PRD**，每份含 背景 / 目标 / 范围 / 设计约束(功能限制) / 逐页字段 / 交互 / 校验 / 状态流转 / 数据对象 / 接口草案 / 风险 / 验收。
> 上层：[[PRD-殡葬AI获客-V1]]（总纲）｜ [[产品规划-殡葬V1]]（路线/成本）｜ [[产品设计-殡葬V1-页面与字段]]（商家端 IA）｜ [[产品设计-殡葬V1-平台后台]]（后台 IA）
> 原型：商家端 `prototype/index.html`｜ 平台后台 `prototype-boss/index.html`（精简版 3 块 + `index-full.html` 完整版备份）
> **本文 = PRD 排期与进度真相**，写完一份回来勾选状态。

---

## 一、双轨产品 PRD 总清单

### 轨 A · 商家端 SaaS（客户使用）
| # | 模块 PRD | 状态 |
|--|--|--|
| A1 | [[PRD_内容创作]] | ✅ 草稿 v1.4（2026-07-05，标杆）——图文预览 + 两级出片确认门引用 + 配图多平台共用规则 |
| A2 | [[PRD_爆款抓取]] | ✅ 草稿 |
| A3 | [[PRD_知识库]] | ✅ 草稿 |
| A4 | [[PRD_数字人]] | ✅ 草稿 |
| A5 | [[PRD_提示词]] | ✅ 现行 v2.0（2026-06-30）——**三层模型**：①系统基线（后台维护·商家端只读可见）②商家微调层（商家可编辑：口吻/品牌/卖点/禁忌）③生成参数（后台按商户默认+覆盖·商家可调·合规锁定）。商家端「提示词微调」页恢复。详见该 PRD §11 |
| A6 | [[PRD_作品库与发布]] | ✅ 草稿 |
| A7 | [[PRD_用量与计费]] | ✅ 草稿 |
| A8 | [[PRD_账号与成员]] | ✅ 草稿 |
| A9 | [[PRD_平台账号绑定]] | ✅ 草稿 |
| A10 | [[PRD_工作台]] | ✅ 草稿 |

### 轨 B · 平台运营后台（我方使用，精简版范围）
> 范围按 2026-06-25 精简决策：**只做账号开通 / 套餐配置 / 合规词库**，其余（看板/成本/工单/授权/任务/审计/供应商/RBAC 等）暂不做 PRD。
| # | 模块 PRD | 状态 |
|--|--|--|
| B1 | [[PRD_后台_账号开通]] | ✅ 草稿 |
| B2 | [[PRD_后台_套餐配置]] | ✅ 草稿 |
| B3 | [[PRD_后台_合规词库]] | ✅ 草稿 |

**双轨小计 13 份，全部完成 ✅（2026-06-25 一轮补齐 11 份）；连同下方 2 份母版级 PRD，合计 15 份全部完成。**

### 母版级通用能力 PRD（非殡葬双轨，2026-06-26 分期定调·一期重心新增）
> 源自 [[AI 获客产品方案 Index]] §十八/§十九。这两份是**跨行业通用能力**定义（供应商全 TBD），不属殡葬 V1 双轨清单。
| # | 能力 PRD | 状态 |
|--|--|--|
| C1 | [[PRD_视频模板化生成与换人换产品]] | ✅ 草稿 v0.7（2026-07-05）——两级出片确认门（分镜首帧图确认）+ Seedance 段改文生图→i2v |
| C2 | [[PRD_多模态知识库底座]] | ✅ 草稿 v0.2（2026-06-28） |

---

## 二、优先级排期（前后端配对写，共享数据对象）

> 原则：**配对模块同批写**——后台与商家端共用同一批数据对象（租户/套餐/词库版本），一起写两边字段才对得齐。批次内按地基→上层推进。

| 批次 | 主题 | 含 PRD | 共享数据对象 | 依赖 |
|--|--|--|--|--|
| **1** | 账号体系（地基） | B1 后台·账号开通 + A8 商家端·账号与成员 | `tenant_company` / `account` / `role` | 无（最先写） |
| **2** | 计费 | B2 后台·套餐配置 + A7 商家端·用量与计费 | `package` / `quota` / `usage_record` / `addon` | 批次1（租户） |
| **3** | 合规 | B3 后台·合规词库 + A5 商家端·提示词 | `lexicon_version` / `lexicon_word` / `prompt` | 批次1 |
| | | ⚠️ 注（2026-06-28）：提示词已收后台，客户端不再有独立提示词模块；本配对的提示词部分以后台能力实现 | | |
| **4** | 内容资产 | A3 知识库 + A4 数字人 | `kb_file` / `kb_fact` / `avatar` / `voice` | 批次1、2（配额） |
| **5** | 产出与发布 | A6 作品库与发布 + A9 平台账号绑定 | `work` / `platform_binding` | A1 内容创作、批次4 |
| **6** | 概览 | A10 工作台 | 聚合只读 | 全部（最后写） |

**当前进度**：批次 1 进行中。

---

## 三、全局约定（各模块 PRD 共用）

- 形态：客户自助 SaaS · Web 端 · 我方后台开通账号（无自助注册）· 行业固定殡葬 · 公司账号多成员
- 架构：引擎/资产分离；数据带 `租户ID × 行业ID` 双维度
- 关键状态由**后台单一驱动**、商家端只读：形象训练态 / 授权态 / 合规词库版本 / 长任务态 / 配额额度（见 [[产品设计-殡葬V1-平台后台]] §八）
- 外部依赖：蝉镜（数字人）/ 蝉妈妈（爆款数据）/ ASR（转写）/ LLM（拆解·生成·合规）/ 抖音发布 API
- 合规红线：禁自爬、私域企微官方、合规预审为提醒非阻断 + 人工复核兜底
- PRD 模板：1 概述 / 2 用户场景 / 3 整体方案(含设计约束) / 4 功能需求(逐字段) / 5 状态流转 / 6 数据对象 / 7 接口草案 / 8 技术考虑 / 9 风险依赖 / 10 验收标准

---

## 四、共用数据对象 schema 契约（单一真相 · 2026-06-30 冻结）

> 落地审查发现:`quota` 等共用表在多份 PRD 中定义不一致(开通写、计费读对不上)。本节冻结**单一真相**,各 PRD 一律指向这里,不再各自定义。

### 4.1 `quota`（配额 · 行级表 · 单一真相）
| 字段 | 类型 | 说明 |
|--|--|--|
| quota_id | str(PK) | |
| company_id | ref | 租户 |
| type | enum | 爆款抓取 / 参考转写 / 内容生成 / 数字人成片(分钟) / 生成形象(个) |
| limit | int | 额度;-1=不限 |
| used | int | 已用 |
| period | str | 月份键(YYYY-MM) |
| source | enum | 套餐 / 增购 |
| enforce | bool | 是否硬限;**V1 仅"生成形象"=true,其余=false(只计数)** |
| cap_estimate | int | "不限(-1)"项的封顶估算量,供套餐满额成本计算 |

**生命周期(三模块统一口径)**：
- **套餐配置**:`package.quota_items`(json `{type:limit}`)是**套餐模板**,只定义"送多少",**不是 quota 本身**。
- **账号开通**:开通/续期时按 `package.quota_items` **实例化成 `quota` 行**(company_id + 当月 period),即"快照"语义。~~`tenant_company.quota_snapshot`(json)取消~~,改为直接写 `quota` 行。
- **用量计费**:读写 `quota` 行(used 累加、limit 校验、增购改 limit)。
- 套餐改动只改 `package`,**不回溯已开通租户的 `quota` 行**(快照隔离)。

### 4.2 `account` / `tenant_company`（账号/公司 · 单一真相）
- 定义见 [[PRD_后台_账号开通]] §6.1/§6.2;[[PRD_账号与成员]] **复用同一张表,不另定义**。
- `account.login` **全局唯一**,后台开通与商家端加成员**走同一唯一性校验服务**(防并发重复)。

### 4.3 `product`（商品/product_id 主表 · 单一真相）
- 定义见 [[PRD_多模态知识库底座]] §13.7:`product_id(PK·UUID) · company_id · external_key · name · aliases · source · created_at`。
- 内容创作/数字人/视频模板/作品库等**按 product_id 引用**,不另建商品表;`kb_fact` / `asset` 持有 `product_id` 外键。

> 🔒 **冻结约定**:以上三组表的 schema 以本节为准;任何 PRD 的局部定义若与此冲突,以本节为单一真相。
